# 性能优化方案

## 背景与结论

经代码链路分析，当前项目页面切换慢（3-5 秒）、按钮无响应感的根本原因是：
**同一次 HTTP 请求内，Supabase 的 auth/数据库调用次数过多，且大量调用是串行而非并行的。**

下面按优先级从高到低排列优化项。每一项都给出了**精确的修改位置、改法和原因**，不涉及业务逻辑的变更，只修复性能问题。

---

## 优先级 P0（改完立即见效，风险极低）

### P0-1：用 React `cache()` 做请求级去重缓存

**影响范围最大、代码改动最小、零风险的一项。**

**原因：**
- `getSessionAppUserRecord(userId)` 在 `requireSessionUser()` 里查一次 `app_users`，`getCurrentAppRole()` 又查一次，导致 `requireAppUser()` 单次调用就有 **2 次 `app_users` 查询 + 2 次 `auth.getUser()`**。
- layout 调用一次 `requireAppUser()`，page 又调用一次，同一请求里合计 **4 次 `app_users` 查询 + 4 次 `auth.getUser()`**。
- `getEffectiveQuestionnaireContext(supabase)` 在 proxy、`getQuestionnaireState()`、`getParticipationState()`、`getDashboardData()` 里各调一次，每次都是 2 个 DB 查询，dashboard 页面单次加载会触发 **6-8 次重复的 `match_batches` + `questionnaire_versions` 查询**。

**做法：**

Next.js App Router 内置了 React 的 `cache()` 函数，**同一个服务端请求内**对相同参数的调用自动复用结果，不同请求之间完全隔离，无需担心数据泄漏。

#### 修改 `src/lib/auth/session.ts`

```typescript
// 在文件顶部加一行 import
import { cache } from "react";

// 把 getSessionAppUserRecord 包成 cached 版本
const getSessionAppUserRecord = cache(async (userId: string) => {
  const supabase = await createServerSupabaseClient();
  const { data, error } = await supabase
    .from("app_users")
    .select("role, account_status, deleted_at")
    .eq("id", userId)
    .maybeSingle();

  if (error) {
    throw error;
  }

  return { appUser: data, supabase };
});

// 同样把 getSessionUser 包成 cached 版本（避免重复调用 auth.getUser()）
const getSessionUser = cache(async () => {
  const supabase = await createServerSupabaseClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  return user;
});
```

注意：原来 `getSessionUser` 是 `export` 的，包成 `cache()` 后继续保持导出即可。`cache()` 包裹不影响函数签名。

#### 修改 `src/features/app/questionnaire-runtime.ts`

```typescript
// 在文件顶部加
import { cache } from "react";

// 把 getEffectiveQuestionnaireContext 的内部实现拆出，用 cache 包住
// 注意：cache() 的 key 是参数，supabase 客户端每次都是新对象无法做 key
// 因此改为：不传 supabase，内部自己创建；或包一个"无参数的版本"供 Server Component 用

// 新增一个请求级缓存的导出函数（Server Component / Server Action 专用）
export const getEffectiveQuestionnaireContextCached = cache(async () => {
  const { createServerSupabaseClient } = await import("@/lib/supabase/server");
  const supabase = await createServerSupabaseClient();
  return getEffectiveQuestionnaireContext(supabase);
});
```

然后把 `src/features/app/data.ts` 中的 `getQuestionnaireState`、`getParticipationState`、`getDashboardData` 里的 `getEffectiveQuestionnaireContext(supabase)` 调用，全部替换成 `getEffectiveQuestionnaireContextCached()`（无需传参）。

proxy 里的 `getEffectiveQuestionnaireContext(supabase)` 仍传 proxy 自己的 supabase 实例（proxy 不走 `cookies()` 创建的客户端，不能用 cache，但 proxy 调用一次即可，保持现状）。

---

### P0-2：精简 `requireAppUser()` 的内部查询

**修改 `src/lib/auth/session.ts`**

现在 `requireAppUser()` 调用链：
```
requireAppUser()
  → requireSessionUser()        ← 1 次 auth.getUser() + 1 次 app_users 查询
  → getCurrentAppRole(user.id)  ← 又 1 次 app_users 查询（重复）
```

配合 P0-1 的 `cache()` 后，重复查询已被消除。但为了更清晰，也可以直接改写：

```typescript
export async function requireAppUser() {
  const user = await requireSessionUser();
  // requireSessionUser 内部已调用 getSessionAppUserRecord（有 cache）
  // 直接复用同一个 cache 结果，不需要单独调 getCurrentAppRole
  const { appUser } = await getSessionAppUserRecord(user.id);

  if (appUser?.role === "admin") {
    redirect("/admin");
  }

  return user;
}

export async function requireAdminUser() {
  const user = await requireSessionUser();
  const { appUser } = await getSessionAppUserRecord(user.id);

  if (appUser?.role !== "admin") {
    redirect("/app");
  }

  return user;
}
```

这样 `requireAppUser()` 和 `requireAdminUser()` 内的 `app_users` 查询都走 cache，一次请求内不管调多少次都只打一次 DB。

---

## 优先级 P1（核心瓶颈，高收益）

### P1-1：收缩 Proxy 的查询范围

**文件：`src/lib/supabase/proxy.ts`**

**原因：**
- 当前 proxy 对 **所有非静态资源请求**都先 `auth.getUser()`（包括 `/`、`/login`、`/register`、`/api/*`）。
- 对 `/app/*` 路径，proxy 还会额外查 `app_users`（含 10 个字段）+ `getEffectiveQuestionnaireContext`（2 个查询）+ `questionnaire_submissions` 查询，共 4-5 个 DB 调用。
- 这些中大部分（questionnaire context + submission）在 page 级别也会再查一遍。

**改法：**

把 proxy 的职责收窄到：**仅做 session refresh + 基础角色路由守卫**，问卷完成状态的检查从 proxy 移出，留给页面自己处理（已有 `requireAppUser()`、page 级 redirect 可以做）。

```typescript
export async function updateSession(request: NextRequest) {
  const env = getPublicEnv();
  const pathname = request.nextUrl.pathname;

  let response = NextResponse.next({ request });

  const supabase = createServerClient<Database>(/* ... cookie setup ... */);

  // ✅ session refresh（必须保留，否则 cookie 无法续期）
  const {
    data: { user },
  } = await supabase.auth.getUser();

  // ✅ 只有 /app 和 /admin 路径才需要进一步检查
  if (!user || (!isAppPath(pathname) && !isAdminPath(pathname))) {
    return response;
  }

  // ✅ 只查路由守卫必要的 3 个字段（去掉 nickname/gender/grade/department/campus/birth_year）
  const profileResult = await supabase
    .from("app_users")
    .select("role, account_status, deleted_at")
    .eq("id", user.id)
    .maybeSingle();

  if (profileResult.error) {
    return response;
  }

  // 删除账号跳转（保留）
  if (
    profileResult.data?.account_status === "deleted" ||
    profileResult.data?.deleted_at
  ) {
    await supabase.auth.signOut();
    return redirectWithCookies(
      request,
      response,
      `/login?error=${encodeURIComponent(DELETED_ACCOUNT_SESSION_ERROR_MESSAGE)}`,
    );
  }

  const role = profileResult.data?.role ?? "user";
  const defaultHomePath = getDefaultHomePathForRole(role);

  // ✅ 角色路由守卫（保留）
  if (role === "admin") {
    if (isAppPath(pathname)) {
      return redirectWithCookies(request, response, defaultHomePath);
    }
    return response;
  }

  if (isAdminPath(pathname)) {
    return redirectWithCookies(request, response, defaultHomePath);
  }

  // ❌ 删除以下这段（profile 完成状态检查 + questionnaire context 查询 + submission 查询）
  // 这部分逻辑移到各页面自己处理，或在 /app/page.tsx 入口页做一次重定向
  // proxy 不应该替所有页面做这个业务判断

  return response;
}
```

**迁移说明：**
- 原来 proxy 做的"profile 未完善 → 重定向到 /app/profile/setup"逻辑，可以移到 `src/app/app/layout.tsx` 里（已经调用了 `requireAppUser()`，再查一次 profile 即可，且有 cache 加持不多打 DB）。
- 或者只在 `src/app/app/page.tsx`（`/app` 根路由）做一次入口重定向检查，其他子页面不做重定向（用户如果直接访问 /app/dashboard 但 profile 未完善，会在页面里看到提示，而不是被 proxy 每次请求都检查）。

---

### P1-2：修复登录/注册按钮的全表扫描

**文件：`src/features/app/actions.ts`**
**函数：`getAuthUserLookup(email)`（第 311 行）**

**原因：**
每次登录/注册，这个函数会循环分页拉取 Supabase Auth 的用户列表（最多 20 页 × 500 = 10,000 用户），在本地做 email 匹配。用户量越大越慢。

**改法（推荐使用数据库函数）：**

在 Supabase 中创建一个 SQL 函数，通过 `auth.users` 视图按 email 精确查找：

```sql
-- 新建迁移文件：supabase/migrations/20260327000001_add_auth_email_lookup_fn.sql

create or replace function public.lookup_auth_user_by_email(p_email text)
returns table (
  user_id uuid,
  email_confirmed_at timestamptz
)
language sql
security definer
set search_path = public
as $$
  select id as user_id, email_confirmed_at
  from auth.users
  where lower(email) = lower(p_email)
  limit 1;
$$;
```

然后修改 `getAuthUserLookup`：

```typescript
async function getAuthUserLookup(email: string): Promise<{
  accountStatus: AppAccountStatus | null;
  registrationStatus: AuthRegistrationStatus;
  userId: string | null;
}> {
  const admin = createAdminSupabaseClient();
  const normalizedEmail = email.trim().toLowerCase();

  // ✅ 一次精确查询替代原来的全表分页扫描
  const { data: authRows, error: authError } = await admin.rpc(
    "lookup_auth_user_by_email",
    { p_email: normalizedEmail },
  );

  if (authError) {
    throw authError;
  }

  const authRow = authRows?.[0] ?? null;

  if (!authRow) {
    return {
      accountStatus: null,
      registrationStatus: "not_found",
      userId: null,
    };
  }

  const { data: appUser, error: appUserError } = await admin
    .from("app_users")
    .select("account_status")
    .eq("id", authRow.user_id)
    .maybeSingle();

  if (appUserError) {
    throw appUserError;
  }

  return {
    accountStatus: (appUser?.account_status ?? null) as AppAccountStatus | null,
    registrationStatus: authRow.email_confirmed_at
      ? "registered_confirmed"
      : "registered_unconfirmed",
    userId: authRow.user_id,
  };
}
```

这样登录/注册的前置查询从 O(用户总数) 变为 O(1)。

---

### P1-3：修复后台用户管理的 Auth N+1

**文件：`src/features/admin/users/data.ts`**
**位置：第 263-273 行（`Promise.all` 里循环调 `getUserById`）**

**原因：**
50 个用户 → 50 次独立 Auth API 调用，并发但仍然消耗大量时间和 Auth API 配额。

**改法：**

Admin 用户列表展示的 email 字段，来源是 `auth.users`。可以用数据库函数一次拿回所有需要的字段：

```sql
-- 新建迁移文件：supabase/migrations/20260327000002_add_batch_auth_users_fn.sql

create or replace function public.get_auth_users_by_ids(p_user_ids uuid[])
returns table (
  user_id uuid,
  email text,
  email_confirmed_at timestamptz,
  last_sign_in_at timestamptz
)
language sql
security definer
set search_path = public
as $$
  select
    id as user_id,
    email::text,
    email_confirmed_at,
    last_sign_in_at
  from auth.users
  where id = any(p_user_ids);
$$;
```

然后修改 `listAdminUsers()` 的最后一段：

```typescript
// ❌ 删除原来的 Promise.all 循环（第 263-273 行）：
// const authUsers = await Promise.all(
//   users.map(async (user) => {
//     const { data, error } = await admin.auth.admin.getUserById(user.id);
//     ...
//   }),
// );

// ✅ 替换为单次 RPC 调用：
const { data: authUsersData, error: authUsersError } = await admin.rpc(
  "get_auth_users_by_ids",
  { p_user_ids: userIds },
);

if (authUsersError) {
  throw authUsersError;
}

const authUserById = new Map(
  (authUsersData ?? []).map((row) => [
    row.user_id,
    { email: row.email, email_confirmed_at: row.email_confirmed_at, last_sign_in_at: row.last_sign_in_at },
  ]),
);
```

同时，`findAuthUserIdsByKeyword` 也需要改（同样是全表扫描）：

```sql
-- 在同一迁移文件中新增：

create or replace function public.find_auth_user_ids_by_email_keyword(p_keyword text)
returns table (user_id uuid)
language sql
security definer
set search_path = public
as $$
  select id as user_id
  from auth.users
  where lower(email) like lower('%' || p_keyword || '%');
$$;
```

```typescript
// 修改 findAuthUserIdsByKeyword 函数：
async function findAuthUserIdsByKeyword(
  admin: ReturnType<typeof createAdminSupabaseClient>,
  keyword: string,
) {
  const normalizedKeyword = keyword.trim().toLowerCase();
  if (!normalizedKeyword) {
    return [];
  }

  // ✅ 一次 DB 查询替代原来的全量翻页扫描
  const { data, error } = await admin.rpc(
    "find_auth_user_ids_by_email_keyword",
    { p_keyword: normalizedKeyword },
  );

  if (error) {
    throw error;
  }

  return (data ?? []).map((row) => row.user_id);
}
```

---

## 优先级 P2（数据库层，稳定收益）

### P2-1：补充缺失的数据库索引

**新建迁移文件：`supabase/migrations/20260327000003_add_performance_indexes.sql`**

```sql
-- 1. match_batches 状态查询（getEffectiveQuestionnaireContext 高频使用）
create index if not exists match_batches_status_signup_end_idx
  on public.match_batches (status, signup_end_at desc);

create index if not exists match_batches_status_publish_idx
  on public.match_batches (status, result_publish_at desc);

-- 2. questionnaire_submissions 用户+版本查询（getQuestionnaireState 高频使用）
create index if not exists questionnaire_submissions_user_version_idx
  on public.questionnaire_submissions (user_id, questionnaire_version_id, submission_no desc);

-- 3. questionnaire_submissions 版本+状态查询（admin 用户管理页使用）
create index if not exists questionnaire_submissions_version_status_idx
  on public.questionnaire_submissions (questionnaire_version_id, status, user_id);

-- 4. batch_participations 批次+用户查询（getParticipationState、getDashboardData 使用）
create index if not exists batch_participations_batch_user_idx
  on public.batch_participations (batch_id, user_id);

-- 5. batch_participations 用户查询（admin 用户管理页使用）
create index if not exists batch_participations_user_updated_idx
  on public.batch_participations (user_id, updated_at desc);

-- 6. match_results 配对+用户查询（match detail 页使用）
create index if not exists match_results_pair_user_idx
  on public.match_results (match_pair_id, user_id, released_at);

-- 7. questionnaire_sections 版本排序查询
create index if not exists questionnaire_sections_version_order_idx
  on public.questionnaire_sections (questionnaire_version_id, sort_order);

-- 8. questionnaire_questions 版本排序查询
create index if not exists questionnaire_questions_version_order_idx
  on public.questionnaire_questions (questionnaire_version_id, sort_order);

-- 9. app_users 创建时间排序（admin 用户列表分页）
create index if not exists app_users_created_at_idx
  on public.app_users (created_at desc);
```

**如何应用：**
在本地 Supabase 环境运行 `supabase db push`，或在 Supabase Dashboard 的 SQL Editor 中直接执行。

---

## 优先级 P3（体感优化，用户感知明显）

### P3-1：把同步邮件发送移出 Action 主链路

**文件：`src/features/app/actions.ts`**
**函数：`createNotification()`（第 190 行）和 `triggerMatchContactAction()`（第 732 行）**

**原因：**
`triggerMatchContactAction` 触发联系方式开放时，会同步等待 2 封 SMTP 邮件发送完成再返回，每封可能耗时 200-2000ms。

**改法（最小改动：fire-and-forget）：**

在 `createNotification()` 里，把邮件发送改为不 await：

```typescript
async function createNotification(input: { /* ... */ }) {
  const admin = createAdminSupabaseClient();
  const { data, error } = await admin
    .from("notifications")
    .insert({ /* ... */ })
    .select("id")
    .single();

  if (error) {
    throw error;
  }

  if (!input.emailTo) {
    return;
  }

  // ✅ 不再 await，改为后台异步发送
  // 邮件发送结果的状态更新也在同一个 promise 链中处理
  sendTransactionalEmail({
    to: input.emailTo,
    subject: input.title,
    text: input.body,
  }).then(async (emailResult) => {
    await admin
      .from("notifications")
      .update({
        email_status: emailResult.ok ? "sent" : "failed",
        emailed_at: emailResult.ok ? new Date().toISOString() : null,
      })
      .eq("id", data.id);
  }).catch((err) => {
    console.error("Background email send failed:", err);
  });

  // ✅ 立即返回，不等邮件完成
}
```

**注意：** 这样改后，邮件可能在极少数情况下（如服务器突然关闭）丢失，但对于当前项目规模是可接受的权衡。如果需要更可靠的方案，见下方备注。

**备注（更可靠方案）：**
参考项目现有的 `/src/app/api/internal/match-batches/email-sync/route.ts` 的模式，把联系信息开放的邮件也接入类似的异步 API 路由处理，即：action 写完数据库状态后，调用内部 API 触发邮件发送，而不是在 action 里直接发。

---

### P3-2：添加 `loading.tsx` 加载骨架屏

**在以下位置新建 `loading.tsx` 文件（内容一样，可以复用一个 Skeleton 组件）：**

- `src/app/app/loading.tsx`（整个 /app 区域通用）
- `src/app/app/dashboard/loading.tsx`
- `src/app/app/participation/loading.tsx`
- `src/app/app/matches/loading.tsx`
- `src/app/admin/loading.tsx`（整个 /admin 区域通用）
- `src/app/admin/users/loading.tsx`

**最简实现示例（可以用项目现有的 UI 组件）：**

```tsx
// src/app/app/loading.tsx
export default function Loading() {
  return (
    <div className="grid gap-6 animate-pulse">
      <div className="h-48 rounded-2xl bg-muted" />
      <div className="h-32 rounded-2xl bg-muted" />
      <div className="h-32 rounded-2xl bg-muted" />
    </div>
  );
}
```

这不能加快真实加载速度，但能消除"页面白屏、按钮冻结"的体感。

---

### P3-3：给高频 Action 按钮加 `useFormStatus` pending 态

**涉及按钮：**
- 仪表盘的"标记已读"按钮（`src/app/app/dashboard/page.tsx`）
- 参与页的"加入本轮"/"取消参与"按钮（`src/app/app/participation/page.tsx`）

**改法：**
将这些按钮提取为客户端组件，使用 `useFormStatus` hook：

```tsx
// src/components/pending-button.tsx
"use client";
import { useFormStatus } from "react-dom";
import { Button } from "@/components/site-ui";

export function PendingButton({
  children,
  ...props
}: React.ComponentProps<typeof Button>) {
  const { pending } = useFormStatus();
  return (
    <Button {...props} disabled={pending}>
      {pending ? "处理中…" : children}
    </Button>
  );
}
```

然后把页面里的 `<Button type="submit">` 替换为 `<PendingButton type="submit">`。

---

## 优化效果预估

| 优先级 | 改动项 | 预期收益 |
|--------|--------|----------|
| P0-1 | `cache()` 请求级去重 | 每页减少 4-8 次重复 DB 查询，dashboard 加载预计快 1-2 秒 |
| P0-2 | `requireAppUser()` 去重查询 | 配合 P0-1，消除 layout + page 双重鉴权开销 |
| P1-1 | 收缩 proxy 查询 | 每次导航减少 2-3 次 DB 查询（移除 questionnaire 上下文检查） |
| P1-2 | 登录/注册 email 精确查询 | 登录/注册按钮响应从 1-5 秒降至 200ms 以内 |
| P1-3 | Admin 用户页去 N+1 | 用户管理页从 5-10 秒降至 500ms 以内 |
| P2-1 | 补充 9 个索引 | 高频查询执行时间降低 50-80% |
| P3-1 | 邮件异步化 | "触发联系"按钮响应从 1-3 秒降至 200ms 以内 |
| P3-2 | 加 loading.tsx | 消除白屏体感，用户感知明显改善 |
| P3-3 | 按钮 pending 态 | 消除"按钮没反应"的错误感知 |

---

## 执行顺序建议

```
第一步：P0-1 + P0-2（session.ts + questionnaire-runtime.ts 加 cache()）
         → 改动最小，效果最大，无风险

第二步：P2-1（执行补充索引的迁移）
         → 纯增量，不改任何代码，只在数据库加索引

第三步：P1-2（getAuthUserLookup 改为精确查询 + 创建对应 SQL 函数）
         → 需要新建迁移 + 修改 actions.ts

第四步：P1-3（listAdminUsers 去 N+1 + findAuthUserIdsByKeyword 改为 DB 查询 + 创建对应 SQL 函数）
         → 需要新建迁移 + 修改 admin/users/data.ts

第五步：P1-1（收缩 proxy 查询）
         → 需要验证 /app 路由的 profile 检查能否正常由 layout 承接

第六步：P3-1（邮件异步化）
         → 改动较小，但需要理解业务可接受的一致性级别

第七步：P3-2 + P3-3（loading.tsx + pending 按钮）
         → 最后做，纯体感优化
```

---

## 注意事项

1. **所有 SQL 函数都需要加 `security definer`**，并在迁移里给 `authenticated` 角色授权：
   ```sql
   grant execute on function public.lookup_auth_user_by_email(text) to authenticated;
   grant execute on function public.get_auth_users_by_ids(uuid[]) to service_role;
   grant execute on function public.find_auth_user_ids_by_email_keyword(text) to service_role;
   ```

2. **`cache()` 函数的作用域是单次 Server 渲染请求**，不跨请求、不跨用户，不需要手动失效，非常安全。

3. **proxy 精简后**，如果有测试用例涉及"proxy 在未完成 profile 时重定向"，需要相应调整测试，因为这个逻辑被移到了 layout 层。

4. **索引迁移**可以在生产环境在线执行，Postgres 的 `CREATE INDEX CONCURRENTLY`（Supabase 支持）不会锁表。如果通过 Supabase 迁移系统部署，可在 SQL 中改用 `CREATE INDEX CONCURRENTLY IF NOT EXISTS`。

5. **邮件 fire-and-forget** 方案在 Vercel/Serverless 环境中，函数执行完毕后未 await 的 Promise 可能被中断。如果部署在 Vercel，请使用 `waitUntil` API 或改用异步 API 路由方案来保证邮件可靠发送。


# 执行顺序

## 第一批：零风险，直接做
这三项不改任何现有逻辑，只是"加"东西：

1. 加数据库索引（P2-1）

新建一个迁移文件，9 条 CREATE INDEX SQL
纯增量，不改任何代码，不可能破坏功能
建议第一个做
2. 加 loading.tsx（P3-2）

新建 6 个文件，每个文件 10 行左右
不影响任何已有逻辑，只是"加载中"时显示骨架屏
改完立即消除白屏体感
3. 加 cache() 去重（P0-1）

在 session.ts 和 questionnaire-runtime.ts 各加 1 行 import { cache } from "react"，把函数定义改为 cache(async () => {...})
React 的 cache() 是请求级别隔离的，不存在跨用户数据污染的问题
改动 3-5 行，但收益最大（消除每页 4-8 次重复 DB 查询）

## 第二批：低风险，需要新建迁移文件
4. 修复登录/注册按钮（P1-2）

写一个 SQL 函数 lookup_auth_user_by_email，再改 actions.ts 里约 30 行
风险点仅在 SQL 函数本身——逻辑简单，测一下登录/注册流程即可验证
5. 修复 Admin 用户页 N+1（P1-3）

写一个 SQL 函数 get_auth_users_by_ids，再改 admin/users/data.ts 约 20 行
只影响后台用户管理页，不影响用户侧
## 可以不做（对核心体验影响很小）
项目	原因
P1-1 精简 proxy	做完 P0-1 的 cache() 后，proxy 里重复的问卷上下文查询已被消除；剩余的 1-2 次查询影响不大，且 proxy 改动逻辑复杂（需要把 profile 检查移到 layout），风险高
P0-2 重构 requireAppUser()	做完 P0-1 之后，重复查询已被 cache() 挡住，这个结构性重构只是"代码更整洁"，没有额外收益
P3-1 邮件异步化	仅影响"触发联系方式开放"这一个按钮，而且该按钮本身不常用；如果部署在 Vercel，fire-and-forget 还有可靠性风险，可以最后再考虑
P3-3 按钮 pending 态	纯视觉优化，做完 P0-1 和 P1-2 之后按钮响应已经很快，体感问题基本解决