# njudate 性能优化方案

> **文档目的**：本文是写给下一个 AI 执行的操作手册。每一项都标注了风险等级、受影响文件和精确的改动方式。执行者应严格按照"已完成 → 低风险 → 中风险"的顺序操作，**高风险/暂缓项不要动**。

---

## 一、3月27日 CPU 突增根因分析

### 背景

Supabase Nano 实例在 2026-03-27 出现 CPU 接近满载（90%+），而前几日在 5%–18%；磁盘 I/O 同步抬头；内存约 45%，不是主因。这是典型的「计算压力突发」，而非内存泄漏。

### 最可能的触发链

根据代码审查，3月27日发生以下几件事的组合最能解释该压力：

#### 1. 匹配批次当日发布（**最高嫌疑**）

系统使用 `pg_cron` 每分钟触发一次 `auto-batch-lifecycle` Edge Function，该函数会自动推进批次状态机，到时间后自动 `publishBatch`。

`publishBatch` 的调用链如下：
```
publishBatch()
  └─ publishBatchCore()          ← SQL: bulk UPDATE match_results, bulk INSERT notifications
       └─ afterPublish()
            └─ syncPendingMatchResultEmails()
                 ├─ 对每个匹配用户：admin.auth.admin.getUserById()  ← N 次串行 Auth API 调用
                 └─ 对每个匹配用户：sendTransactionalEmail()         ← N 次串行 SMTP 连接
```

即使只有 20–30 对匹配（40–60 个用户），在 Nano 实例上串行执行 40–60 次 Auth API + 40–60 次 SMTP 就足以把 CPU 打满。

#### 2. 结果公布后用户集中登录（**高度相关**）

结果公布后，用户涌入登录查看结果。**每次登录都触发 `getAuthUserLookup`**，该函数当前仍使用分页扫描整个 Auth 用户表：

```typescript
// src/features/app/actions.ts：getAuthUserLookup（当前代码，尚未修复）
for (let page = 1; page <= 20; page++) {
  await admin.auth.admin.listUsers({ page, perPage: 500 });  // 每次 1 个 HTTP 往返
  // 找到就停，找不到最多 20 次
}
```

10 个用户同时登录 = 最多 200 次串行 Auth API 调用并发。这直接打满 Nano 实例的 CPU。

> ⚠️ **关键发现**：`lookup_auth_user_by_email` SQL 函数已于 3月26日在迁移文件中创建，但应用代码（`getAuthUserLookup`）**从未被更新来使用它**。这是当前最大的待修复漏洞。

#### 3. 3月27日的迁移部署本身

迁移 `20260327100000_update_match_result_notification_body.sql` 重建了 `publish_match_batch` 函数。在 Supabase 上部署迁移会短暂锁表，对 Nano 实例有明显影响。

### 结论

| 因素 | 当日是否发生 | CPU 贡献 |
|------|------------|---------|
| `syncPendingMatchResultEmails` 串行 N 次 Auth+SMTP | 极可能 | 高 |
| 用户集中登录触发 `getAuthUserLookup` 分页扫描 | 极可能 | 高 |
| 迁移部署锁表 | 确定 | 中 |

---

## 二、已完成的优化（勿重复，已生效）

以下内容已通过代码审查**确认存在**，执行者跳过这些部分：

### 数据库层（2026-03-26 迁移）

迁移 `20260326120000_add_performance_indexes.sql` 已添加：
- `match_batches(status, signup_end_at DESC)` ✅
- `match_batches(status, result_publish_at DESC)` ✅
- `questionnaire_submissions(user_id, questionnaire_version_id, submission_no DESC)` ✅
- `questionnaire_submissions(questionnaire_version_id, status, user_id)` ✅
- `batch_participations(batch_id, user_id)` ✅
- `batch_participations(user_id, updated_at DESC)` ✅
- `match_results(match_pair_id, user_id, released_at)` ✅
- `questionnaire_sections(questionnaire_version_id, sort_order)` ✅
- `questionnaire_questions(questionnaire_version_id, sort_order)` ✅
- `app_users(created_at DESC)` ✅

### SQL 函数（2026-03-26 迁移）

- `public.lookup_auth_user_by_email(p_email text)` ✅（已建，**但应用层未使用**）
- `public.get_auth_users_by_ids(p_user_ids uuid[])` ✅（已建且已使用）
- `public.find_auth_user_ids_by_email_keyword(p_keyword text)` ✅（已建且已使用）

### 应用层

- `src/features/app/questionnaire-runtime.ts`：`getEffectiveQuestionnaireContext` 已包裹 `cache()` ✅
- `src/features/admin/users/data.ts`：Admin 用户页已改为 `get_auth_users_by_ids` RPC，消除了 N+1 ✅
- `src/features/admin/users/data.ts`：关键字搜索已改为 `find_auth_user_ids_by_email_keyword` RPC ✅
- `src/features/app/actions.ts`：`createNotification` 的邮件发送已是 fire-and-forget（`void sendTransactionalEmail(...).then(...)` 不 await）✅
- `src/lib/auth/session.ts`：`getSessionUser` 和 `getSessionAppUserRecord` 已包裹 `cache()` ✅

### Loading 骨架（已存在）

- `src/app/app/loading.tsx` ✅
- `src/app/app/dashboard/loading.tsx` ✅
- `src/app/app/matches/loading.tsx` ✅
- `src/app/app/participation/loading.tsx` ✅
- `src/app/admin/loading.tsx` ✅
- `src/app/admin/users/loading.tsx` ✅

---

## 三、待完成的优化

---

### 【紧急·低风险】Fix-1：修复 `getAuthUserLookup`

**文件**：`src/features/app/actions.ts`

**当前问题**：函数 `getAuthUserLookup`（约第 311–361 行）通过 `admin.auth.admin.listUsers({ page, perPage: 500 })` 分页扫描所有用户来匹配 email，最多 20 页 × 500 = 10,000 用户，每次最多 20 个 HTTP 往返。此函数在每次登录和每次注册时都被调用。

**已有基础**：`lookup_auth_user_by_email` SQL 函数已存在于数据库（`20260326132000_add_auth_email_lookup_fn.sql`），直接通过 `WHERE lower(email) = lower(p_email)` 精确查询 `auth.users`，O(1) 而非 O(用户总数)。

**风险说明**：只改这一个函数内部逻辑，函数签名、返回值类型、调用方不变。登录/注册流程行为完全不变，仅速度不同。风险极低。

**改动方式**：

将 `getAuthUserLookup` 函数体（`src/features/app/actions.ts`，约第 311–361 行）**整体替换**为：

```typescript
async function getAuthUserLookup(email: string): Promise<{
  accountStatus: AppAccountStatus | null;
  registrationStatus: AuthRegistrationStatus;
  userId: string | null;
}> {
  const admin = createAdminSupabaseClient();
  const normalizedEmail = email.trim().toLowerCase();

  const { data, error } = await admin.rpc(
    "lookup_auth_user_by_email" as never,
    { p_email: normalizedEmail } as never,
  );

  if (error) {
    throw error;
  }

  const matched = (
    data as Array<{ user_id: string; email_confirmed_at: string | null }> | null
  )?.[0] ?? null;

  if (!matched) {
    return {
      accountStatus: null,
      registrationStatus: "not_found",
      userId: null,
    };
  }

  const { data: appUser, error: appUserError } = await admin
    .from("app_users")
    .select("account_status")
    .eq("id", matched.user_id)
    .maybeSingle();

  if (appUserError) {
    throw appUserError;
  }

  return {
    accountStatus: (appUser?.account_status ?? null) as AppAccountStatus | null,
    registrationStatus: matched.email_confirmed_at
      ? "registered_confirmed"
      : "registered_unconfirmed",
    userId: matched.user_id,
  };
}
```

**改动说明**：
- `admin.rpc("lookup_auth_user_by_email" as never, ...)` 与 `admin/users/data.ts` 中已有的 `admin.rpc("get_auth_users_by_ids" as never, ...)` 用法完全一致
- 类型断言 `as never` 是因为该函数尚未出现在自动生成的类型文件中，与现有代码保持一致
- 原来的 `for` 循环和 `maxPages`、`perPage` 变量全部删除

**验证方法**：
1. 注册一个新邮箱 → 应该能正常跳转到确认邮件页
2. 用已注册邮箱注册 → 应该提示"邮箱已注册"
3. 登录正确密码 → 正常登录
4. 登录错误密码 → 正常报错

---

### 【建议·低风险】Fix-2：补充缺失的 loading.tsx

**问题**：以下页面在数据加载期间无骨架屏，用户体感到白屏：

| 缺失文件 | 对应页面 | 数据加载量 |
|---------|---------|----------|
| `src/app/app/questionnaire/loading.tsx` | 问卷填写页 | 重（全部题目） |
| `src/app/app/profile/loading.tsx` | 个人资料页 | 中 |
| `src/app/app/settings/loading.tsx` | 设置页 | 轻 |
| `src/app/app/matches/[matchId]/loading.tsx` | 匹配详情页 | 中 |

**风险**：纯新增文件，不改任何现有逻辑，零风险。

**骨架屏示例**（以问卷页为例，其余参照此模式）：

```tsx
// src/app/app/questionnaire/loading.tsx
export default function QuestionnaireLoading() {
  return (
    <div className="mx-auto max-w-2xl animate-pulse space-y-6 px-4 py-8">
      <div className="h-6 w-1/3 rounded bg-muted" />
      <div className="h-4 w-2/3 rounded bg-muted" />
      <div className="space-y-4 rounded-xl border p-6">
        <div className="h-5 w-1/2 rounded bg-muted" />
        <div className="h-10 w-full rounded bg-muted" />
        <div className="h-10 w-full rounded bg-muted" />
        <div className="h-10 w-full rounded bg-muted" />
      </div>
      <div className="h-10 w-28 rounded-full bg-muted" />
    </div>
  );
}
```

每个文件参照这个模板写，结构尽量接近对应页面布局（卡片、标题、按钮位置），但不需要完全一致。

---

### 【建议·中风险】Fix-3：消除 `getParticipationState` 内的二次问卷上下文查询

**文件**：`src/features/app/data.ts`

**当前问题**：`getParticipationState`（约第 399 行）的执行路径如下：

```
getParticipationState(userId)
  ├─ getEffectiveQuestionnaireContext(supabase)      ← 查询 match_batches + questionnaire_versions（2次DB）
  ├─ match_batches 查一次
  ├─ getCurrentProfile(userId)
  ├─ getQuestionnaireState(userId)                   ← 内部再次调用 getEffectiveQuestionnaireContext(newSupabase)
  │     = 又查一次 match_batches + questionnaire_versions（再2次DB，因为client不同导致cache miss）
  │     + questionnaire_sections（1次DB）
  │     + questionnaire_questions（1次DB）
  │     + questionnaire_submissions（1次DB）
  └─ batch_participations 查一次
```

**核心问题**：`getEffectiveQuestionnaireContext` 虽有 `cache()`，但以 supabase client 对象为 key。`getParticipationState` 和 `getQuestionnaireState` 各自创建新 client，导致 cache miss，`match_batches` + `questionnaire_versions` 各被多查一次。

**改动方向**：

`getParticipationState` 调用 `getQuestionnaireState(userId)` 的目的只有一个：判断用户是否**已提交**问卷（以确认是否满足参与条件）。它并不需要 sections、questions 等完整结构。

**最小改动方案**：在 `getParticipationState` 内，不调用 `getQuestionnaireState`，改为直接查询提交状态：

在约第 486 行，把：

```typescript
const questionnaire = await getQuestionnaireState(userId);
if (
  questionnaire.windowStatus !== "open" ||
  !isQuestionnaireCompletedStatus(questionnaire.status)
) {
  return {
    ...
    status: "unavailable",
    reason: getQuestionnaireParticipationRequirement({
      status: questionnaire.status,
      windowStatus: questionnaire.windowStatus,
    }),
  } satisfies ParticipationState;
}
```

替换为（`questionnaireContext` 在此处已经取到了）：

```typescript
// windowStatus 已从 questionnaireContext 中获取
if (questionnaireContext.windowStatus !== "open") {
  return {
    batchId: batch.id,
    label: batch.label,
    resultPublishAt: batch.result_publish_at,
    signupEndAt: batch.signup_end_at,
    matchRunAt: batch.match_run_at,
    status: "unavailable",
    reason: "当前报名已截止，请等待下一轮开放。",
  } satisfies ParticipationState;
}

// 直接查提交状态，避免重新拉取整个 questionnaire（sections、questions 全集）
const { data: submission, error: submissionError } = await supabase
  .from("questionnaire_submissions")
  .select("id")
  .eq("user_id", userId)
  .eq("questionnaire_version_id", questionnaireContext.versionId)
  .eq("status", "submitted")
  .maybeSingle();

if (submissionError) {
  throw submissionError;
}

if (!submission) {
  return {
    batchId: batch.id,
    label: batch.label,
    resultPublishAt: batch.result_publish_at,
    signupEndAt: batch.signup_end_at,
    matchRunAt: batch.match_run_at,
    status: "unavailable",
    reason: "请先完成并提交问卷，再报名本轮匹配。",
  } satisfies ParticipationState;
}
```

**改动收益**：
- 消除 2 次 DB 查询（match_batches + questionnaire_versions 的重复取数）
- 消除 3 次 DB 查询（sections、questions、submissions 的完整拉取，改为只查 1 次 submissions）
- 净节约约 4 次 DB 查询，每次 `/app/participation` 加载受益

**风险说明**：
- 功能逻辑不变：判断条件（问卷窗口是否开放 + 用户是否已提交）完全等价
- `getQuestionnaireParticipationRequirement` 的 reason 文案由两种情况简化为两条固定文案，如需完全一致可保留原调用
- 仅影响 `/app/participation` 页的加载数据，不影响任何写操作
- **改前请确认**：`/app/participation` 页的 UI 不依赖 `getParticipationState` 返回 questionnaire 的完整结构（sections、questions 等）——从代码看它只使用 `ParticipationState` 类型，不含问卷结构，所以没有问题

**验证方法**：
1. 未提交问卷的用户访问参与页 → 显示"请先完成问卷"提示
2. 已提交问卷且报名窗口开放的用户 → 显示参与按钮
3. 报名窗口关闭后 → 显示"已截止"提示

---

## 四、暂缓/高风险项（当前阶段不建议执行）

### 【暂缓】H1：`syncPendingMatchResultEmails` 并行化

**位置**：`src/lib/matching/batch-runner.ts`

**问题**：批次发布后，`syncPendingMatchResultEmails` 对每个匹配用户串行执行 `getUserById()` + `sendTransactionalEmail()`。40 个用户 × 200ms/次 = 8 秒+。这是批次发布时 CPU 突增的直接原因。

**为何暂缓**：
- 这是批次发布的后台过程，**不阻塞任何用户操作**（`createNotification` 里的用户通知邮件已是 fire-and-forget）
- 该函数通过 `/api/internal/match-batches/email-sync` 路由触发，属于内部流程
- 在 Vercel/Next.js 边缘环境中，将 `syncPendingMatchResultEmails` 改为并发需要注意 Supabase 连接池上限
- 改动涉及 `lifecycle-core.ts`（核心基础设施），风险高

**如果确认要做**，方向是：
- 使用 `get_auth_users_by_ids` 一次批量取所有用户邮箱（而非逐个 `getUserById`）
- 使用 `Promise.allSettled` 并行发送邮件，设置合理并发上限（如 5-10 并发）

### 【暂缓】H2：精简 Proxy 层逻辑

**位置**：`src/lib/supabase/proxy.ts`

**问题**：proxy 中对所有已认证用户访问 `/app`、`/admin` 时，会查询 `app_users` 和 `questionnaire_submissions`，逻辑较重。

**为何暂缓**：
- `session.ts` 的 `cache()` 已消除 layout/page 层的重复查询，proxy 层的这次查询是路由守卫必要开销
- proxy 路由逻辑一旦出错会导致重定向死循环，风险极高
- 现有查询均有索引覆盖，实际延迟在可接受范围

### 【暂缓】H3：Proxy 中 `getEffectiveQuestionnaireContext` 缓存穿透

**问题**：proxy 使用自建 Supabase 客户端（基于 `request.cookies`），而 page 使用 `createServerSupabaseClient()`（基于 `next/headers`）。因为 `cache()` 以 client 对象为 key，两层的调用不共享缓存，即每次请求 `match_batches` 会被查两次（proxy 一次 + page 一次）。

**为何暂缓**：
- 这 2 次查询有完整索引支持，耗时极短（< 5ms）
- 修复需要重构 `getEffectiveQuestionnaireContext` 的接口（去掉 supabase 参数），需要同步修改 proxy.ts、data.ts，改动面较大
- 收益相对有限

---

## 五、实施顺序

```
第一步（立即，风险零）：
  Fix-2：补充 loading.tsx（4个文件，纯新增）

第二步（立即，风险极低）：
  Fix-1：修复 getAuthUserLookup（改 1 个函数体，约30行，验证登录注册流程即可）

第三步（完成前两步验证无误后）：
  Fix-3：消除 getParticipationState 内的二次查询（改 data.ts 约25行）
  → 改完后完整测试 /app/participation 页面的各种状态

第四步（有余力时）：
  H1：syncPendingMatchResultEmails 并行化
  → 批次邮件发送优化，需要充分测试 match 流程
```

---

## 六、改动影响范围速查

| 文件 | 改动类型 | 影响范围 |
|------|---------|---------|
| `src/features/app/actions.ts` | 修改 `getAuthUserLookup` 函数体 | 登录、注册 |
| `src/app/app/questionnaire/loading.tsx` | 新建 | 问卷页加载体感 |
| `src/app/app/profile/loading.tsx` | 新建 | 资料页加载体感 |
| `src/app/app/settings/loading.tsx` | 新建 | 设置页加载体感 |
| `src/app/app/matches/[matchId]/loading.tsx` | 新建 | 匹配详情页加载体感 |
| `src/features/app/data.ts` | 修改 `getParticipationState` 内约25行 | 参与页加载 |

---

## 七、当前已知限制（不用修、接受即可）

1. **Server Action 的鉴权查询无法共享 render 缓存**：`requireAuthenticatedClient()` 在每次 Server Action 调用时仍会执行 `auth.getUser()` + `app_users` 查询。这是 Next.js App Router 的架构限制（Action 和 render 不在同一个 request context），无法用 `cache()` 跨越，接受即可，该查询本身已有索引覆盖。

2. **`questionnaire_versions` 无 status 搜索索引**：已有 `questionnaire_versions_single_published_idx` partial index `WHERE status = 'published'`，覆盖最高频的回退查询路径，足够用。

3. **`auth.users` 的 email 无 lower() 函数索引**：`lookup_auth_user_by_email` 用 `lower(email) = lower(p_email)` 匹配，在 `auth.users` 表上没有对应的函数索引。Supabase 的 `auth` schema 不建议直接加索引。但此函数做的是精确匹配（O(log n) 或顺序扫描），相比原来的分页扫描（O(总用户数)）仍是巨大改进，接受即可。
