# 技术栈

请使用以下技术栈，不要换栈：

- Next.js 15+（App Router）
- TypeScript
- Tailwind CSS
- shadcn/ui
- Supabase
  - Supabase Auth
  - Supabase Postgres
  - Row Level Security
  - Supabase Edge Functions（如有必要）
  - pg_cron 或 Supabase 定时任务
- Vercel 部署
- 邮件发送建议用 Resend（如果你认为更合适也可封装成 provider interface，默认实现用 Resend）
- 表单校验：zod
- ORM 可选：
  - 优先直接使用 Supabase SQL migration + typed query
- 测试：
  - 核心单元测试
  - 至少包含匹配算法测试和权限测试样例
- 代码规范：
  - eslint
  - prettier
  - 严格类型
  - 清晰目录结构

# 开发步骤

第一阶段，先把前端做完整，但用假数据驱动。
把所有关键页面都做成可点击、可切换状态的版本，例如：

未登录 / 已登录

问卷未完成 / 已完成

未报名本周 / 已报名

有匹配结果 / 无匹配结果 / 等待结果

未联系 / 已联系

管理后台有数据 / 空状态

第二阶段，把前端里的“数据契约”先定下来。
比如每个页面需要什么字段，先写成 mock schema：

用户资料对象长什么样

问卷题目和答案的数据结构是什么

匹配结果对象包含哪些字段

通知列表是什么格式

批次列表是什么格式

这样你后面接 Supabase 或其他后端时，只需要把 mock data 换成真实接口，不用重做页面。

第三阶段，再接入后端。
顺序建议是：

登录态与用户会话

用户资料

问卷保存与读取

本周参与状态

匹配结果读取

联系 TA

后台数据

最后才是定时匹配和真实邮件

这个顺序会比较顺。

