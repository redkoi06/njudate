# 问卷设计说明

这份说明是给问卷设计工作人员的。目标不是做一份泛化的“心理测试”或“校园调研”，而是产出一份可以直接用于平台匹配的结构化问卷。

## 1. 先说清楚：这份问卷是拿来做匹配的

- 系统会把两位用户的答案做结构化比较，计算相似度，再决定是否进入候选配对。
- 因此，题目必须服务于“是否适合进一步认识”，而不是服务于展示感、趣味感，或收集大量无法参与计算的信息。
- 问卷的核心价值是提升匹配质量，不是单纯增加题目数量。

## 2. 你们负责设计什么，不负责设计什么

- 你们负责设计“深度偏好问卷”。
- 你们不需要把基础资料再写进问卷。以下字段由资料页单独采集，不属于问卷内容：
  - 性别
  - 年级
  - 院系
  - 校区
  - 出生年份
- 最终交付的是一整版完整问卷，不是零散题目。
- 这一版问卷还必须能落成一份完整 JSON，因为后台导入只接受 JSON。

## 3. 系统的硬约束

### 3.1 题型只支持三种

- `single`：单选题，只能选 1 个选项。
- `multiple`：多选题，可以选多个选项。
- `scale`：量表题，在整数区间内选择一个值，例如 1 到 5 或 1 到 7。

以下形式当前都不支持：

- 开放题
- 填空题
- 排序题
- 矩阵题
- 条件跳题
- 上传题

### 3.2 问卷结构固定

一版问卷必须包含：

- `title`：问卷标题
- `description`：问卷说明
- `matchingPolicy`：匹配规则配置
- `sections`：问卷分组

每个 `section` 必须包含：

- `code`：分组编码，全问卷唯一
- `title`：分组标题
- `subtitle`：分组副标题，可空
- `description`：分组说明，可空
- `sortOrder`：排序值，整数
- `questions`：题目列表，至少 1 题

每个 `question` 必须包含：

- `questionCode`：题目编码，全问卷唯一
- `kind`：`single` / `multiple` / `scale`
- `prompt`：题目标题
- `helperText`：补充说明，可空
- `isRequired`：是否必答
- `sortOrder`：排序值，整数
- `weight`：题目权重，可不填；不填时系统按题型默认权重补齐

题型附加字段：

- `single` / `multiple` 必须有 `options`
- `scale` 必须有：
  - `scaleMin`
  - `scaleMax`
  - `scaleLeftLabel`
  - `scaleRightLabel`

### 3.3 编码和选项的要求

- `section.code` 不能重复。
- `question.questionCode` 不能重复。
- 同一题里的 `options.id` 不能重复。
- `options.label` 不能为空。
- `scaleMin` 必须小于 `scaleMax`。
- `sortOrder` 必须是整数。

建议：

- `code`、`questionCode`、`options.id` 使用稳定、简短、可读的英文或英文连字符命名，例如 `campus-life`、`q-weekend-plan`、`sports`。
- 不要在编码里使用随意中文句子。

## 4. 题目应该怎么设计

### 4.1 优先设计“稳定、可比较、会影响相处体验”的题

优先考虑这些维度：

- 日常节奏
- 社交方式
- 活动偏好
- 沟通习惯
- 关系推进节奏
- 相处边界

这些维度更适合做匹配，因为它们：

- 双方都能回答
- 容易比较
- 对现实相处有影响

不建议把大量题目放在这些方向：

- 纯展示型题目
- 很临时、很受当天状态影响的题目
- 很难比较高低或远近的题目
- 只是为了“有趣”但不会进入匹配决策的题目

### 4.2 三种题型分别适合什么问题

`single` 适合：

- 明确只有一个主偏好的问题
- 选项之间互斥的问题
- 分类很清楚的问题

例如：

- 你更喜欢哪种相处节奏？
- 你更倾向哪种周末安排？

设计要求：

- 选项必须互斥，不要让用户觉得两个都像自己。
- 选项要处于同一语义层级，不要把“大类”和“小类”混在一起。
- 不要把非常细碎的差异硬塞进单选题。

`multiple` 适合：

- 用户可以同时拥有多个偏好的问题
- 适合用“标签集合”描述的问题

例如：

- 周末愿意做哪些活动？
- 希望第一次聊天更容易从哪些话题开始？

设计要求：

- 选项必须平行，不要出现包含关系明显的选项。
- 不要把“运动”和“羽毛球”放在同一道多选题里，这会让相似度计算失真。
- 不要让“全选”变成常态，否则区分度会很差。

`scale` 适合：

- 程度型、连续型偏好
- 强弱、快慢、主动程度、计划程度等连续维度

例如：

- 你更偏好热闹还是安静？
- 你希望关系推进更顺其自然还是更有计划？

设计要求：

- 左右两端必须清晰、对立、可理解。
- 量表值必须是整数区间。
- 同一版问卷里，量表方向尽量保持一致，减少误填。

## 5. 为什么题要这样设计：系统实际怎么打分

### 5.1 基础资料先做过滤和加权

当前系统的固定规则是：

- 性别是硬过滤条件：只允许异性匹配。
- 年级、院系、校区可以配置为“相同加分”。
- 出生年份按年龄差做惩罚，差距越大，分数越低。

注意：

- 这些基础资料已经在资料页采集，不要再写成问卷题。

### 5.2 三种题型的打分方式

- `single`：双方答案相同，得 1 分；不同，得 0 分。
- `multiple`：按“交集 / 并集”计算相似度。
- `scale`：按答案距离计算相似度，距离越近，分数越高。

这意味着：

- `single` 更适合做强区分的分类题。
- `multiple` 更适合做并列标签题，但选项必须平行。
- `scale` 更适合做连续偏好题。

### 5.3 最终分数怎么形成

- 每道题都有权重。
- 最终总分是基础资料得分和问卷题得分的加权平均，再换算成 0 到 100 的整数分。
- 如果双方可比较的题目数量低于 `minimumComparableQuestions`，这对候选会被直接丢弃。
- 如果最终总分低于 `minimumPairScore`，这对候选也会被直接丢弃。

这直接影响设计：

- 核心题尽量设为必答。
- 不要把大量关键题做成可跳过，否则很多候选对会因为“可比较题数不够”直接失效。
- 宁可题目少而准，也不要堆很多低质量题。

## 6. 题目设计红线

请不要这样设计：

- 不要设计开放题，因为系统不会用于匹配计算。
- 不要把基础资料重复写进问卷。
- 不要把两个维度塞进同一题，例如“你是否主动且幽默”。
- 不要使用双重否定、绕口句、强价值判断句。
- 不要让选项长度差异过大，或一部分是短词、一部分是长段落。
- 不要让多选题选项存在明显包含关系。
- 不要为了“显得专业”增加大量用户难以理解的抽象题。
- 不要设计只有极少数人才能理解的圈层黑话题目。

## 7. 权重怎么理解

- 如果没有特别明确的业务理由，可以先使用系统默认权重。
- 只有那些确实决定相处体验的题，才建议单独提高权重。
- 不要把很多题都标成高权重，否则权重就失去区分意义。
- `matchingPolicy` 里的阈值和默认权重，应该由产品、运营和工程一起确认，不建议由问卷设计单独拍板。

## 8. 推荐的出题工作方式

建议按这个顺序设计：

1. 先确定要比较的核心维度，而不是先堆题。
2. 每个维度只保留最能区分的题。
3. 再决定每道题应该用 `single`、`multiple` 还是 `scale`。
4. 最后统一检查：
   - 题目是否真的能参与匹配
   - 选项是否平行
   - 文案是否清晰
   - 关键题是否应设为必答

## 9. 交付给工程时至少要有这些内容

- 问卷标题
- 问卷说明
- 分组结构
- 每道题的：
  - 题目编码
  - 题型
  - 题目文案
  - 补充说明
  - 是否必答
  - 排序值
  - 选项或量表定义
  - 如需单题特殊权重，则明确标出
- 整体匹配策略建议：
  - 最低匹配分
  - 最少可比较题数
  - 各题型默认权重
  - 是否对年级、院系、校区、年龄差加分或惩罚

## 10. JSON 骨架示意

下面是工程侧最终可导入的结构骨架。你们设计内容时，必须保证最终能整理成这个结构。

```json
{
  "title": "2026 春季深度问卷",
  "description": "用于本阶段匹配的正式问卷版本",
  "matchingPolicy": {
    "minimumPairScore": 60,
    "profileFilters": [
      {
        "field": "gender",
        "mode": "opposite_required"
      }
    ],
    "profileScoring": [
      {
        "field": "grade",
        "mode": "same_bonus",
        "weight": 0.6
      },
      {
        "field": "department",
        "mode": "same_bonus",
        "weight": 0.4
      },
      {
        "field": "campus",
        "mode": "same_bonus",
        "weight": 0.2
      },
      {
        "field": "birth_year",
        "mode": "distance_penalty",
        "maxGap": 4,
        "weight": 0.5
      }
    ],
    "questionScoring": {
      "singleDefaultWeight": 1,
      "multipleDefaultWeight": 1.2,
      "scaleDefaultWeight": 1.5,
      "minimumComparableQuestions": 6
    }
  },
  "sections": [
    {
      "code": "campus-life",
      "title": "校园生活",
      "subtitle": "日常节奏与活动偏好",
      "description": "这一组主要比较双方的日常生活方式。",
      "sortOrder": 1,
      "questions": [
        {
          "questionCode": "q-campus-rhythm",
          "kind": "single",
          "prompt": "你更喜欢哪种校园节奏？",
          "helperText": "只能选一个选项。",
          "isRequired": true,
          "sortOrder": 1,
          "options": [
            { "id": "slow", "label": "慢节奏" },
            { "id": "balanced", "label": "平衡一些" },
            { "id": "fast", "label": "快节奏" }
          ]
        },
        {
          "questionCode": "q-weekend-plan",
          "kind": "multiple",
          "prompt": "周末你通常愿意做哪些事？",
          "helperText": "可以选择多个选项。",
          "isRequired": true,
          "sortOrder": 2,
          "options": [
            { "id": "study", "label": "学习" },
            { "id": "sports", "label": "运动" },
            { "id": "walk", "label": "散步" }
          ]
        },
        {
          "questionCode": "q-planning-level",
          "kind": "scale",
          "prompt": "你希望关系推进更接近哪一端？",
          "helperText": "在区间内选一个整数。",
          "isRequired": true,
          "sortOrder": 3,
          "scaleMin": 1,
          "scaleMax": 7,
          "scaleLeftLabel": "顺其自然",
          "scaleRightLabel": "更有计划"
        }
      ]
    }
  ]
}
```

## 11. 一句话总结

请把这份问卷设计成一套“能稳定比较双方偏好、能直接进入算法打分、能落成结构化 JSON”的匹配问卷，而不是一份泛化调研问卷。
