# 数据库设计

## 概览

当前方案使用：

- `14` 张业务表
- `1` 张 Supabase 内置认证表 `auth.users`

设计原则只有四条：

- 只存业务事实，不额外存前端页面拼出来的状态
- 已发布问卷禁止原地修改，历史记录必须始终可解释
- 一次成功匹配是一个成对事实，联系触发只能有一个 pair 级真相来源
- 关键一致性必须落到数据库约束、约束触发器或单事务写入函数，不能依赖前端自觉

---

## 问卷展示与存储策略

问卷采用以下方式：

- 前端按数据库中的问卷定义动态渲染
- 后端把问卷版本作为一等实体管理
- 用户答案按“某个问卷版本下的一份提交快照”存储

### 前端展示方式

- 用户端每次读取唯一一个“当前已发布问卷版本”
- 返回结构包含：分组、题目、题型、排序、必答、选项、提示文案
- 前端只硬编码题型渲染器，不硬编码题目内容
- 当前支持的题型就是需求里已有的：
  - `text`
  - `single`
  - `multiple`
  - `scale`

### 存储方式

- 问卷版本单独建表：`questionnaire_versions`
- 分组与题目都挂在具体版本下
- 用户提交记录绑定具体版本
- 答案存到 `questionnaire_submissions.answers_json`

### 必须守住的规则

- 同一时刻最多只有一个 `published` 问卷版本
- 已发布版本禁止原地修改
- 修改问卷时，复制旧版本生成新 `draft` 版本
- 用户正式提交后生成一条不可变的 `submitted` 记录
- 用户后续修改问卷时，必须从最近一次 `submitted` 记录派生新 `draft`，不能回写旧提交
- `match_batches` 必须绑定具体 `questionnaire_version_id`
- 用户报名后，批次使用的问卷依据必须锁定，不随用户后续继续编辑而漂移

---

## 匹配与联系建模

这里必须把“匹配结果”和“联系行为”拆开建模：

- 一次用户可见结果是 `match_results` 中的一条记录
- 一次真实成功配对是 `match_pairs` 中的一条记录
- 一次成功配对会对应两条 `match_results`，分别属于配对双方
- 联系触发、联系信息开放、联系失败重试、结果过期，这些都是 pair 级事实，只能写在 `match_pairs`
- `match_results` 只保存用户视角下的结果展示数据，例如匹配理由、预览文案、是否已查看

这样做的原因只有一个：

- “同一结果不能重复触发联系”必须有单一真相来源，不能把一份联系状态拆成两行用户记录分别维护

---

## 表清单总览

| 表名 | 作用 |
| --- | --- |
| `app_users` | 用户资料、账号状态、通知设置 |
| `questionnaire_versions` | 问卷版本主表 |
| `questionnaire_sections` | 某个问卷版本下的分组定义 |
| `questionnaire_questions` | 某个问卷版本下的题目定义 |
| `questionnaire_submissions` | 用户问卷草稿与正式提交版本 |
| `match_batches` | 每周匹配批次 |
| `batch_participations` | 用户参加某一批次的报名记录 |
| `match_pairs` | 某一批次中的一对成功匹配及其联系事实 |
| `match_results` | 某一批次下用户收到的结果视图 |
| `notifications` | 站内通知与邮件发送记录 |
| `announcements` | 平台公告 |
| `service_requests` | 联系我们、举报、导出申请、删除申请 |
| `app_configs` | 平台运营配置与策略 |
| `operation_logs` | 关键操作日志 |

---

## 不单独建表的状态

以下状态不建议单独建一张“状态表”，而是由业务事实推导：

### 用户状态

由以下信息组合推导：

- `app_users.account_status`
- 资料是否填写完整
- 是否存在至少一条当前问卷版本下的 `submitted` 记录
- 当前批次是否已报名
- 最近一次 `match_results` 与对应 `match_pairs` 的联系状态

### 问卷状态

由 `questionnaire_submissions` 推导：

- 无记录：`未开始`
- 有 `draft` 且没有 `submitted`：`填写中` 或 `已保存草稿`
- 最新记录是 `submitted`：`已提交`
- 存在较新的 `draft`，且较旧的最新正式记录仍为 `submitted`：`已更新待下一轮生效`

### 本周参与状态

由 `match_batches` 和 `batch_participations` 推导：

- 无参与记录：`未报名`
- 已报名且未截止：`已报名`
- 截止后锁定：`已截止不可修改`
- 资料或问卷不满足要求：`不可报名`

### 匹配结果状态

由 `match_results.status`、`match_results.viewed_at` 与 `match_pairs.contact_status` 推导：

- `pending`
- `matched`
- `unmatched`
- `error`
- `expired`
- 是否已查看
- 是否已联系

---

## 分表设计

### 1. `app_users`

用途：承接业务用户信息，和 `auth.users` 一一对应。

关键字段：

- 主键与关联：`id uuid pk references auth.users(id)`
- 权限：`role text check(user|admin) default 'user'`
- 账号状态：`account_status text check(active|restricted|delete_requested|deleted)`
- 状态原因：`account_status_reason text`
- 基础资料：`nickname text`, `department text`, `major text`, `grade text`, `gender text`
- 匹配资料：`target_preference text`, `bio text`, `interests text[]`
- 展示控制：`show_nickname boolean`
- 非关键通知设置：
  - `notify_match_result boolean`
  - `notify_weekly_reminder boolean`
  - `notify_platform_digest boolean`
- 时间字段：`created_at`, `updated_at`, `deleted_at`

必须约束：

- 学校邮箱以 `auth.users.email` 为唯一真相来源，`app_users` 不重复存 `email`
- 联系触发通知、账号异常通知等关键通知不允许被用户关闭
- “展示哪些字段给对方看”由结果快照和平台策略决定，不在 `app_users` 中拆成多组长期布尔开关

### 2. `questionnaire_versions`

用途：管理问卷的草稿、发布和归档版本。

关键字段：

- `id uuid pk`
- `version_no int unique`
- `status text check(draft|published|archived)`
- `title text`
- `description text`
- `created_by uuid fk`
- `published_at timestamptz null`
- `archived_at timestamptz null`
- `created_at`
- `updated_at`

必须约束：

- 同一时刻最多只有一个 `status = 'published'`
- 一旦进入 `published`，该版本下的分组和题目只允许读，不允许改

### 3. `questionnaire_sections`

用途：定义某个问卷版本下的分组。

关键字段：

- `id uuid pk`
- `questionnaire_version_id uuid fk`
- `code text`
- `title text`
- `subtitle text`
- `description text`
- `sort_order int`
- `created_at`
- `updated_at`

必须约束：

- 同一版本下 `code` 唯一
- 不再单独设置分组级发布状态；是否生效只由所属问卷版本决定

### 4. `questionnaire_questions`

用途：定义某个问卷版本下的题目内容，支持题型、排序和选项配置。

关键字段：

- `id uuid pk`
- `questionnaire_version_id uuid fk`
- `section_id uuid fk`
- `question_code text`
- `kind text check(text|single|multiple|scale)`
- `prompt text`
- `helper_text text`
- `placeholder text`
- `is_required boolean`
- `options_json jsonb`
- `scale_min int null`
- `scale_max int null`
- `scale_left_label text`
- `scale_right_label text`
- `sort_order int`
- `created_at`
- `updated_at`

必须约束：

- 同一版本下 `question_code` 唯一
- `(section_id, questionnaire_version_id)` 必须指向同版本下的 `questionnaire_sections`
- 不再单独设置题目级发布状态；是否生效只由所属问卷版本决定
- `options_json` 只用于 `single` / `multiple`
- `scale` 题必须同时有 `scale_min` 和 `scale_max`
- 同一题目的选项 `id` 在该版本内必须稳定，不能在已发布版本里重排后复用旧 `id`

### 5. `questionnaire_submissions`

用途：保存用户问卷草稿和正式提交版本。

关键字段：

- `id uuid pk`
- `user_id uuid fk`
- `questionnaire_version_id uuid fk`
- `submission_no int`
- `status text check(draft|submitted)`
- `answers_json jsonb`
- `submitted_at timestamptz null`
- `created_at`
- `updated_at`

必须约束：

- `answers_json` 用 `question_code` 做 key
- 单选存 `option_id`
- 多选存 `option_id[]`
- 评分存数字
- 文本存字符串
- 同一用户、同一问卷版本下，`submission_no` 唯一
- 同一用户、同一问卷版本下，同时最多只有一条 `draft`
- `submitted` 记录不可变
- 用户再次修改问卷时，必须新建 `draft`，不能回写任何历史 `submitted`

### 6. `match_batches`

用途：记录每周匹配批次及其运行状态，并锁定该批次使用的问卷版本。

关键字段：

- `id uuid pk`
- `code text unique`
- `label text`
- `questionnaire_version_id uuid fk`
- `signup_start_at timestamptz`
- `signup_end_at timestamptz`
- `match_run_at timestamptz`
- `result_publish_at timestamptz`
- `status text check(draft|open|locked|processing|published|cancelled|failed)`
- `paused_reason text`
- `notes text`
- `created_at`
- `updated_at`
- `processed_at timestamptz null`
- `published_at timestamptz null`

必须约束：

- 批次一旦离开 `draft`，`questionnaire_version_id` 不允许再改
- 打开的批次只能绑定一个已发布过的问卷版本

### 7. `batch_participations`

用途：记录用户是否参加某一轮匹配，以及该轮实际使用的问卷与资料快照。

关键字段：

- `id uuid pk`
- `batch_id uuid fk`
- `questionnaire_version_id uuid fk`
- `user_id uuid fk`
- `status text check(joined|cancelled|locked)`
- `questionnaire_submission_id uuid fk`
- `profile_snapshot_json jsonb`
- `joined_at timestamptz null`
- `cancelled_at timestamptz null`
- `locked_at timestamptz null`
- `created_at`
- `updated_at`

必须约束：

- 同一 `batch` 下每个用户最多一条记录
- `questionnaire_version_id` 必须等于所属 `match_batches.questionnaire_version_id`
- `questionnaire_submission_id` 必须属于同一 `user_id`、同一 `questionnaire_version_id`，且其 `status = 'submitted'`
- `profile_snapshot_json` 必须存报名当时用于匹配和结果展示的基础资料快照
- 报名与取消必须通过单事务入口完成，入口内必须同时校验：
  - 用户已登录
  - 账号未受限
  - 基础资料完整
  - 存在有效 `submitted` 问卷
  - 当前时间处于报名窗口内

### 8. `match_pairs`

用途：记录某一批次中的一对成功匹配，以及这对匹配后续的联系事实。

关键字段：

- `id uuid pk`
- `batch_id uuid fk`
- `left_participation_id uuid fk`
- `right_participation_id uuid fk`
- `left_user_id uuid fk`
- `right_user_id uuid fk`
- `contact_status text check(idle|confirming|triggered|failed|completed) default 'idle'`
- `contact_triggered_by uuid null`
- `contact_triggered_at timestamptz null`
- `contact_payload_json jsonb null`
- `contact_error text null`
- `expires_at timestamptz null`
- `created_at`
- `updated_at`

必须约束：

- 只有成功匹配时才创建 `match_pairs`
- 同一条 pair 内两侧用户必须不同
- `left_participation_id` 与 `right_participation_id` 必须属于同一 `batch`
- `left_participation_id` 与 `right_participation_id` 必须按固定顺序存储，例如强制 `left_participation_id < right_participation_id`，避免同一对匹配出现镜像重复记录
- 同一批次内，一个 participation 只能出现在一条 pair 中
- `contact_triggered_by` 非空时，必须是这对 pair 双方用户中的一方
- 联系方式开放结果只写一次，只写在 `match_pairs.contact_payload_json`
- “联系 TA” 的真正去重键是 `match_pairs.id`，不是任一侧的 `match_results.id`

### 9. `match_results`

用途：记录用户在某一批次中的结果视图，包括是否匹配成功、展示给用户的理由以及是否已查看。

关键字段：

- `id uuid pk`
- `batch_id uuid fk`
- `user_id uuid fk`
- `participation_id uuid fk`
- `match_pair_id uuid null`
- `status text check(pending|matched|unmatched|error|expired)`
- `counterpart_snapshot_json jsonb null`
- `score int null`
- `preview_text text null`
- `reasons text[] null`
- `shared_signals text[] null`
- `released_at timestamptz null`
- `viewed_at timestamptz null`
- `error_detail text null`
- `created_at`
- `updated_at`

必须约束：

- 同一 `batch` 下每个用户最多一条结果记录
- `(participation_id, batch_id, user_id)` 必须指向同一条 `batch_participations`
- `status in ('matched', 'expired')` 时，`match_pair_id` 必须非空
- `status in ('pending', 'unmatched', 'error')` 时，`match_pair_id` 必须为空
- `status in ('matched', 'expired')` 时，必须同时具备 `counterpart_snapshot_json`、`score` 和 `3` 至 `5` 条 `reasons`
- `counterpart_snapshot_json` 只存联系前允许展示的有限资料
- 联系状态不再写在 `match_results`，统一从 `match_pairs.contact_status` 推导

### 10. `notifications`

用途：保存站内通知，并记录邮件发送情况。

关键字段：

- `id uuid pk`
- `user_id uuid fk`
- `category text`
- `title text`
- `body text`
- `level text check(info|success|warning)`
- `is_read boolean`
- `read_at timestamptz null`
- `email_status text check(not_needed|pending|sent|failed) default 'not_needed'`
- `emailed_at timestamptz null`
- `source_type text`
- `source_id uuid null`
- `created_at`

必须约束：

- 用户只能修改已读状态，不能改邮件发送状态
- 邮件发送结果由系统写入，不允许前端直接更新

### 11. `announcements`

用途：管理平台公告，支持发布、下线和按受众展示。

关键字段：

- `id uuid pk`
- `title text`
- `body text`
- `eyebrow text`
- `audience text check(public|user|admin|all)`
- `status text check(draft|published|archived)`
- `starts_at timestamptz`
- `ends_at timestamptz`
- `created_by uuid fk`
- `created_at`
- `updated_at`
- `published_at timestamptz null`
- `archived_at timestamptz null`

### 12. `service_requests`

用途：统一承接用户主动发起的服务请求。

覆盖范围：

- 联系我们
- 举报或不适反馈
- 数据导出申请
- 删除账号申请

关键字段：

- `id uuid pk`
- `request_type text check(consultation|report|export_data|delete_account)`
- `user_id uuid null`
- `sender_name text null`
- `sender_email text`
- `topic text null`
- `message text null`
- `priority text check(normal|urgent) default 'normal'`
- `status text check(open|processing|resolved|closed) default 'open'`
- `admin_reply text null`
- `internal_note text null`
- `handled_by uuid null`
- `handled_at timestamptz null`
- `created_at`
- `updated_at`
- `completed_at timestamptz null`

必须约束：

- 匿名咨询只允许通过 `server action` 或 `route handler` 入库
- 普通用户不能写 `admin_reply`、`internal_note`、`handled_by`、`handled_at`、`completed_at`

### 13. `app_configs`

用途：存放平台策略与运营配置。

关键字段：

- `config_key text pk`
- `value_json jsonb`
- `description text`
- `updated_by uuid null`
- `updated_at`

建议配置项：

- `registration_open`
- `weekly_participation_open`
- `allowed_email_domains`
- `match_schedule_text`
- `contact_flow_text`
- `feature_flags`
- `repeat_match_cooldown_days`

### 14. `operation_logs`

用途：记录关键业务操作，满足最小必要的审计和运营追踪要求。

关键字段：

- `id uuid pk`
- `actor_user_id uuid null`
- `actor_role text`
- `target_user_id uuid null`
- `action_type text`
- `entity_type text`
- `entity_id uuid null`
- `payload_json jsonb null`
- `created_at`

适用场景：

- 用户触发联系
- 管理员限制账号 / 恢复账号
- 管理员处理咨询
- 用户发起删除申请
- 用户发起导出申请

---

## 核心约束

### 唯一约束

- `questionnaire_versions(version_no)`
- `questionnaire_sections(questionnaire_version_id, code)`
- `questionnaire_questions(questionnaire_version_id, question_code)`
- `questionnaire_submissions(user_id, questionnaire_version_id, submission_no)`
- `questionnaire_submissions(user_id, questionnaire_version_id) where status = 'draft'`
- `batch_participations(batch_id, user_id)`
- `match_results(batch_id, user_id)`
- `match_pairs(batch_id, left_participation_id)`
- `match_pairs(batch_id, right_participation_id)`

### 跨表一致性约束

- `questionnaire_questions(section_id, questionnaire_version_id)` 必须指向同版本的 `questionnaire_sections`
- `batch_participations(batch_id, questionnaire_version_id)` 必须指向同一条 `match_batches`
- `batch_participations(questionnaire_submission_id, user_id, questionnaire_version_id)` 必须指向同一条 `questionnaire_submissions`
- `match_results(participation_id, batch_id, user_id)` 必须指向同一条 `batch_participations`
- `match_pairs` 两侧 participation 必须都来自同一批次，且用户不同
- 每一条 `match_pairs` 必须对应且只对应两条 `match_results(status = 'matched' 或 'expired')`

### 不可变约束

- `questionnaire_versions.status = 'published'` 后，该版本内容不可修改
- `questionnaire_submissions.status = 'submitted'` 后，该记录不可修改
- `match_batches` 离开 `draft` 后，`questionnaire_version_id` 不可修改
- `match_pairs.contact_triggered_at` 一旦写入，不能被重复覆盖为第二次成功触发

### 必须走受控事务入口的业务动作

以下动作不能开放为“用户直接改表行”：

- 发布问卷版本
- 保存问卷草稿
- 正式提交问卷
- 报名本周匹配
- 取消本周匹配
- 批量生成 `match_pairs` 与 `match_results`
- 触发“联系 TA”

这些动作必须通过 `RPC`、`server action`、`route handler` 或等价的单事务后端入口完成。

---

## RLS 权限建议

### 普通用户

- `app_users`：只能读自己的资料；只能更新自己的基础资料、`show_nickname` 和非关键通知开关；不能改 `role`、`account_status`、`deleted_at`
- `questionnaire_versions`：只能读 `published`
- `questionnaire_sections`：只能读 `published` 版本下的数据
- `questionnaire_questions`：只能读 `published` 版本下的数据
- `questionnaire_submissions`：只能读自己的记录；草稿保存与正式提交必须走受控事务入口；不能修改任何 `submitted` 记录
- `batch_participations`：只能读自己的记录；报名/取消必须走受控事务入口；不能直接写 `status`、`locked_at`、`profile_snapshot_json`
- `match_pairs`：普通用户不直接写；如产品查询需要，只能读自己所在的 pair
- `match_results`：只能读自己的记录；`viewed_at` 建议通过专用动作写入，不直接开放任意更新
- `notifications`：只能读自己的记录；只能标记已读
- `service_requests`：只能创建和读取自己发起的请求；不能写后台处理字段
- `announcements`：只能读自己可见范围内且处于发布时间窗口的数据

### 管理员

- 可以管理 `questionnaire_versions`
- 可以管理 `questionnaire_sections`
- 可以管理 `questionnaire_questions`
- 可以管理 `match_batches`
- 可以管理 `announcements`
- 可以管理 `app_configs`
- 可以处理 `service_requests`
- 可以查看聚合后的用户、批次、咨询、运营数据

必须额外强调：

- 管理后台默认不直接开放“浏览所有用户原始问卷全文”
- 若确有高权限审核场景，必须走带审计日志的专用查询入口，而不是给后台页面直接开表级全量读取

### 系统角色

- `service role` / `cron` / `edge function` 负责写入 `match_pairs`
- `service role` / `cron` / `edge function` 负责写入 `match_results`
- 系统负责写入通知邮件发送状态
- 系统负责写入 `operation_logs`

### 匿名用户

- 不建议直接开放任何业务表的匿名写权限
- “联系我们”入口建议走 `server action` 或 `route handler` 后再写入数据库
